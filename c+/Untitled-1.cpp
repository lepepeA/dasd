#include <iostream>
using namespace std;

int main() {
    double num1, num2; 
    char operador;
count << "dar el primer numero para una operacion: ";
cin >> ejemplo1;
count << "dar el segundo numero para la operacion: ";
cin >> ejemplo2;
count << "dar el operador que quieras: ";
cin >> operador;

switch (operador) {
    case '+':
    resultado = num1 + num2 
    break;
case '-';
resultado = num1 - num2
break;

case '*';
resultado = num1 *num2

case '/';
if (num2 != 0)
resultado = num1 / num2
break;

}
}